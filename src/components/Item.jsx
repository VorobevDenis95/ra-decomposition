import React from 'react';
// отображение содержания постов, все пропсы являются необязательными, но если они содержатся в item, то попадают в разметку
export default function Item(item) {
    const{label, text, href, time, promt, logo}=item
  return (
    <div>
      
    </div>
  );
}
